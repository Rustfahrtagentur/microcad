-- Test --
  mod
  ../doc/structure/functions.md:66

-- Code --

// module math
mod math {
    // pow cannot be called from outside math
    fn pow( x: Scalar, n: Integer ) {
        if n == 1 {
            x   // return x
        } else {
            x * pow(x, n-1) // return recursive product
        }
    }

    // square is callable from outside math
    pub fn square(x: Scalar) {
        // call internal pow
        pow(x, 2.0)
    }
}

// call square in math
math::square(2.0);
math::pow(2.0, 5);  // error: pow is private

-- Parse Error --
Parser error:  --> 7:9
  |
7 |         } else {
  |         ^---
  |
  = expected COMMENT, identifier, array_element_access, tuple_element_access, attribute_access, add, subtract, multiply, divide, union, intersection, power_xor, greater_than, less_than, greater_equal, less_equal, equal, near, not_equal, and, or, xor, if_binary_op, else_binary_op, method_call, marker, or body
-- Test Result --
FAIL (TODO)
