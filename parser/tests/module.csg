module donut(outer_radius: scalar, inner_radius: scalar) {
    difference() {
        circle(r = self.outer_radius);
        circle(r = self.inner_radius);
    }
}

export("test.svg") {
    stroke_color: color = #00FF00;    
    donut(5.0mm, 2.0mm);
} 