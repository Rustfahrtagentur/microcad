use std::geo2d::*;
use std::ops::*;

#[color = "red"]
sketch drill_plate(size = 100mm, hole_size = 10mm) {
    l = size/2 - hole_size;
    rect(size) - circle(d = hole_size, c = (x = [-l,l], y = [-l,l]));
}

drill_plate();
