use std::geo2d::*;
use std::ops::*;

#[color = "red"]
sketch drill_plate(size = 100mm, hole_size = 10mm) {
    difference() {
        l = size/2 - hole_size;
        rect(size);
        translate(x = [-l,l], y = [-l,l])
            circle(diameter = hole_size) 
    }
}

drill_plate();
