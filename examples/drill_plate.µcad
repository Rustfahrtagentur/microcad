use std::geo2d::*;
use std::ops::*;

#[color = "red"]
sketch DrillPlate(size = 100mm, hole_size = 10mm) {
    {
        l = size/2 - hole_size;
        std::geo2d::Rect(size);
        Circle(diameter = hole_size).
            translate(x = [-l,l], y = [-l,l])
    }.subtract()
}


DrillPlate();
